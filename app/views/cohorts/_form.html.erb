

<%= simple_form_for(@cohort) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :demo_day_date %>
    <%= f.association :city %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, class:'btn' %>
  </div>
<% end %>
